# Cloudflare Pages configuration for Next.js SSR
name = "charades-generator-pages"

# Pages configuration
pages_build_output_dir = ".next"

[build]
command = "npm run build"

[build.environment]
NODE_VERSION = "20"
NPM_FLAGS = "--production=false"